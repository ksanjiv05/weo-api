export enum STATUS {
  PENDING = 0,
  LIVE = 1,
  DELETED = 2,
  PUSHED = 3,
}

export enum OFFER_STATUS {
  PENDING = 1,
  LIVE = 2,
  PUSHED = 3,
  DELETED = 4,
  SOLD = 5,
  EXPIRED = 6,
  REJECTED = 7,
}
